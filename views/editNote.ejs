<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Note</title>
  <!-- Include Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="container mt-5">
  <h1 class="mb-4">Edit Note</h1>
  <form method="POST" action="/notes/<%= vehicleId %>/<%= index %>?_method=PUT" class="form-group">
    <div class="mb-3">
      <label for="date" class="form-label">Date:</label>
      <input type="date" name="date" id="date" class="form-control" value="<%= note.date %>" required>
    </div>
    <div class="mb-3">
      <label for="odometer" class="form-label">Odometer:</label>
      <input type="number" name="odometer" id="odometer" class="form-control" value="<%= note.odometer %>" required>
    </div>
    <div class="mb-3">
      <label for="note" class="form-label">Note:</label>
      <textarea name="note" id="note" class="form-control" required><%= note.note %></textarea>
    </div>
    <div class="mb-3">
      <label for="cost" class="form-label">Cost:</label>
      <input type="number" name="cost" id="cost" class="form-control" value="<%= note.cost || '' %>" step="0.01">
    </div>
    <button type="submit" class="btn btn-primary">Save Changes</button>
    <a href="/notes/<%= vehicleId %>" class="btn btn-secondary">Cancel</a>
  </form>
</body>
</html>
